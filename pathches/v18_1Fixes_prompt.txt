You are an expert Python engineer and research-code reviewer working on a single-file experiment harness for a TMLR-style paper on Semantic Data Normal Forms.

Your task is to generate ONE downloadable git patch file named:

  unified_sdnf_experiment_hybrid_v18_1_dbnf_dedup_c4_fix.patch

The patch must apply cleanly with:

  git apply unified_sdnf_experiment_hybrid_v18_1_dbnf_dedup_c4_fix.patch

Target file to patch:

  unified_sdnf_experiment_hybrid_v18_1.py

Reference files available for comparison:

  unified_sdnf_experiment_hybrid_v12.py
  unified_sdnf_experiment_hybrid_v13.py
  unified_sdnf_experiment_hybrid_v14.py
  unified_sdnf_experiment_hybrid_v15.py
  unified_sdnf_experiment_hybrid_v16.py
  unified_sdnf_experiment_hybrid_v17.py

Relevant v18_1 result/audit files available:

 
  decisions_audit_v18_1.csv
  field_evidence_audit_v18_1.csv
  normal_forms_and_claims_audit_v18_1.csv
  out_audit_v18_1.txt
  run_manifest_v18_1.json
  scale_timing_drift_audit_v18_1.csv
  summary_audit_v18_1.json
  srs_evolved_schema_v18_1.compact.json

Do not ask clarifying questions. Inspect the attached source and previous versions, then produce the patch file directly.

BACKGROUND / CURRENT OBSERVED ISSUES

The v18_1 DBNF version drift run was executed in paper mode with:

  --dbnf_mode version_drift
  embedding_backend=sentence-transformers
  candidate_backend_requested=auto
  candidate_backend_used=hnsw
  hnsw_available=True

However, the v18_1 claims output still reports:

  C3_dbnf_drift = NOT_EVALUATED

with an evidence message like:

  "DBNF scaffold/diagnostic present; no explicit drift ground truth evaluated in v18 default run"

This is wrong or at least misleading when --dbnf_mode version_drift is explicitly selected. v18_1 should not treat version_drift mode as a default unevaluated run.

The alias evaluation also reports:

  raw_predicted_pair_count = 76
  unique_predicted_pair_count = 38
  no_duplicate_pairs_in_predictions = False

This indicates production/discovery or other audit-track duplicates are being surfaced as duplicate prediction failures. Metrics should be computed from deduplicated canonical pairs, while duplicate audit-track entries should remain explainable in audit outputs.

C4 context safety also reports NOT_SUPPORTED due to repeated examples:

  Plaid.transaction_id::UPI.txn_id
  Plaid.transaction_id::UPI.txn_id

The decision audit indicates this pair was accepted as:

  Plaid.transaction_id <-> UPI.txn_id
  CanonicalNode = transaction_identifier
  FamilyA = identifier:transaction
  FamilyB = identifier:upi_transaction
  RoleA = transaction_identifier
  RoleB = upi_transaction_identifier
  PromotionRule = alias_overlap_promotion
  CMNF = FAIL

This must be fixed or transparently qualified. It is not acceptable for v18_1 to silently accept the pair while C4 remains ambiguous. Either:
  A) make this mapping explicitly supported under a clear, narrow transaction-identifier compatibility rule, or
  B) keep it accepted only as a qualified/exception mapping and mark C4 as PARTIALLY_SUPPORTED with explicit evidence explaining the qualification.

Prefer option B unless the code already has enough formal policy infrastructure to support option A cleanly.

PATCH REQUIREMENTS

Only modify:

  unified_sdnf_experiment_hybrid_v18_1.py

Do not modify test data, audit CSVs, prior versions, README files, or ground truth files.

Do not add new dependencies.

Do not exceed the existing output-file budget or create new standalone audit files. If new diagnostics are needed, add them into existing outputs:
  - summary_audit_v18_1.json
  - alias_evaluation_audit_v18_1.csv
  - normal_forms_and_claims_audit_v18_1.csv
  - out_audit_v18_1.txt
  - scale_timing_drift_audit_v18_1.csv
  - decisions_audit_v18_1.csv if already generated by existing writer path

Preserve backward-compatible CLI behavior.

Implement changes minimally and locally. Avoid large unrelated refactors.

REQUIREMENT 1 — DBNF VERSION_DRIFT FIX

Inspect v12, v13, v15, and v17 for prior DBNF-related logic, including:
  - DriftEvalResult or equivalent structures
  - DBNF version_drift mode
  - migration mode separation
  - controlled drift handling, if any
  - DBNF claim/support summaries
  - scale_timing_drift audit rows

Patch v18_1 so that:

1. If args.dbnf_mode == "version_drift":
   - Execute an actual DBNF version drift diagnostic/evaluation path.
   - Populate summary_audit_v18_1.json normal_form_summaries["DBNF"] with:
       status: SUPPORTED or PARTIALLY_SUPPORTED or SCAFFOLDED_NOT_EVALUATED, depending on measurable evidence available.
       mode: "version_drift"
       evaluated: true
       model_before or base_model
       model_after or drift_model/version_model, if available
       embedding_backend
       attribute_count
       drift_metric fields that are actually computed, such as:
         mean_cosine_shift
         max_cosine_shift
         detected_drift_count
         tau or threshold
         measurable
       reason explaining whether explicit drift ground truth exists.
   - Set C3_dbnf_drift in normal_forms_and_claims_audit_v18_1.csv based on actual available evidence:
       If no explicit drift ground truth is provided but a version drift diagnostic is executed, do NOT say "v18 default run".
       Use PARTIALLY_SUPPORTED or SCAFFOLDED_NOT_EVALUATED with precise evidence, not NOT_EVALUATED.
       Only use SUPPORTED if explicit drift ground truth or a defensible controlled evaluation exists and metrics meet the implemented criteria.
   - Make scale_timing_drift_audit_v18_1.csv include DBNF diagnostic metrics already produced by the code, without creating a new file.

2. If args.dbnf_mode is default/none/disabled/migration:
   - Do not execute version_drift DBNF evaluation.
   - Preserve the requested message exactly:
       DBNF: no explicit drift ground truth evaluated in v18 run for default and migration mode - feature available for version mode only
   - For migration mode, make clear that migration is utility/governance mode only and not the C3 paper claim evaluation.
   - Keep C3_dbnf_drift as NOT_EVALUATED for default and migration modes only.

3. Do not break existing migration behavior if present.
   - migration should continue to work as a utility mode, but must not satisfy C3.

4. Add comments that explain:
   - version_drift is the only mode eligible for C3.
   - migration is explicitly not counted as C3 evidence.
   - default mode intentionally reports NOT_EVALUATED.

REQUIREMENT 2 — DEDUPLICATE PREDICTED PAIRS AND EXPLAIN AUDIT TRACK DUPLICATES

Patch v18_1 so that metric predictions are deduplicated before computing TP/FP/FN/self-checks.

Important behavior:

1. Predicted metric pairs should be built from ACCEPT_MERGE decisions but normalized into a canonical Pair key:
   - stable order
   - slug/normalized names
   - no self-pairs
   - no duplicate pairs

2. Raw accepted decisions may still include production/discovery duplicates. That is okay, but they must not cause:
   no_duplicate_pairs_in_predictions = False
   if the unique metric-pair set itself is clean.

3. Add or restore helper logic similar to v16/v17:
   - collect raw accepted pair list
   - create unique predicted pair set
   - detect duplicate raw audit tracks using Counter
   - expose duplicate explanation separately

4. In alias_evaluation_audit_v18_1.csv:
   - raw_predicted_pair_count should remain the raw accepted decision count.
   - unique_predicted_pair_count should remain the deduplicated count.
   - no_duplicate_pairs_in_predictions should be True if the unique metric prediction set has no duplicates.
   - Add a metric row if the current CSV row format supports it:
       duplicate_raw_audit_pair_count
       duplicate_raw_audit_pair_examples
       duplicate_raw_audit_pair_explanation
     If adding rows is easier than columns, use Metric/Value rows:
       duplicate_raw_audit_pair_count, <count>
       duplicate_raw_audit_pair_examples, <semicolon examples>
       duplicate_raw_audit_pair_explanation, "Raw audit tracks may contain production/discovery duplicates; strict metrics use deduplicated canonical pairs."

5. In summary_audit_v18_1.json self_checks:
   - no_self_pairs_in_predictions = true/false based on deduped set.
   - no_duplicate_pairs_in_predictions = true based on deduped metric set.
   - Add:
       raw_audit_duplicate_pairs_detected: true/false
       raw_audit_duplicate_pair_count: int
       raw_audit_duplicate_pair_examples: [...]
       duplicate_pair_policy: "Metrics are computed from deduplicated canonical pairs; duplicate production/discovery audit tracks are retained only for lineage explainability."

6. In out_audit_v18_1.txt:
   - Replace/augment “Self-check no duplicate predicted pairs” to clarify:
       Self-check no duplicate metric predicted pairs: True
       Raw audit duplicate pairs detected: <count> duplicate audit-track entries retained for lineage explainability

7. Ensure TP/FP/FN metrics use unique pairs only.
   - Do not let production/discovery duplicates double count.
   - Do not change schema-truth expected pairs.
   - Do not alter ground truth.

REQUIREMENT 3 — PLAID.transaction_id ↔ UPI.txn_id / C4 CONTEXT SAFETY

Patch v18_1 so that C4 context safety is no longer ambiguous.

Current issue:
  Plaid.transaction_id and UPI.txn_id are accepted as transaction_identifier.
  But families differ:
    identifier:transaction
    identifier:upi_transaction
  CMNF shows FAIL.
  C4 is NOT_SUPPORTED due to cross-context examples.

Implement a transparent policy.

Preferred behavior:

1. Add a narrow helper or policy check, for example:
   is_qualified_transaction_identifier_bridge(attr_a, attr_b, decision)
   or
   classify_context_safety_exception(decision)

2. The helper should classify Plaid.transaction_id ↔ UPI.txn_id as a QUALIFIED_TRANSACTION_IDENTIFIER_BRIDGE only if all of the following hold:
   - both semantic families are identifier transaction subtypes, e.g. identifier:transaction and identifier:upi_transaction
   - canonical node is transaction_identifier
   - roles are transaction identifier compatible
   - no hard veto exists
   - decision was accepted through alias/canonical evidence
   - the pair is not payer/payee, debtor/creditor, account/card PAN, customer ID, routing number, VPA role-sensitive, or other high-risk identifier subtype

3. Do not mark the pair as an unqualified safe cross-context merge.
   Instead:
   - either remove it from "unsafe cross-context merge" count and count it under "qualified_context_bridge_count", OR
   - keep it in cross-context count but mark C4 as PARTIALLY_SUPPORTED with an explicit qualified exception.

Preferred C4 output:
  - If all cross-context examples are only qualified transaction-identifier bridges:
      C4_context_safety = PARTIALLY_SUPPORTED
      Evidence JSON should include:
        accepted_merge_count
        raw_cross_context_merge_count
        unsafe_cross_context_merge_count
        qualified_context_bridge_count
        qualified_context_bridge_examples
        qualification: "Plaid.transaction_id::UPI.txn_id is treated as a qualified transaction-identifier bridge, not an unqualified context-safe merge; C4 is partially supported pending explicit CMNF matrix policy or reviewer-approved ground truth."
  - If there are truly unsafe cross-context merges remaining:
      C4_context_safety = NOT_SUPPORTED
      Evidence should distinguish unsafe examples from qualified examples.

4. Update normal_forms_and_claims_audit_v18_1.csv C4 row accordingly.

5. Update summary_audit_v18_1.json cross_context section with:
   - raw_cross_context_merge_count
   - unsafe_cross_context_merge_count
   - qualified_context_bridge_count
   - qualified_context_bridge_examples
   - unsafe_cross_context_examples
   - context_safety_policy

6. Update decisions audit fields if practical:
   - add or populate AuditFlags for this pair with:
       qualified_transaction_identifier_bridge
       c4_qualified_exception
   - Do not falsely mark CMNF PASS unless a formal CMNF matrix entry is also added.
   - If CMNF remains FAIL, make the claim evidence explain that C4 is only partially supported.

7. Optional but acceptable:
   - Add a formal CMNF_COMPATIBILITY_MATRIX entry for transaction_identifier with conservative min evidence and subtype rules.
   - If doing so, ensure it does not accidentally allow unrelated identifiers like customer_id, account_id, routing_number, payment_intent_id, order_id, message_id, end_to_end_id, STAN, PAN, or bank account number.
   - Even with a matrix entry, keep the C4 evidence transparent.

VALIDATION EXPECTATIONS

After applying the patch and rerunning equivalent DBNF version_drift command:

1. C3_dbnf_drift should no longer say NOT_EVALUATED for --dbnf_mode version_drift.
   It should report PARTIALLY_SUPPORTED or SUPPORTED only if evidence exists, otherwise SCAFFOLDED_NOT_EVALUATED with precise version_drift wording.
   It must not say "v18 default run" in version_drift mode.

2. For default/migration DBNF modes:
   C3_dbnf_drift should remain NOT_EVALUATED with exact message:
     DBNF: no explicit drift ground truth evaluated in v18 run for default and migration mode - feature available for version mode only

3. alias_evaluation_audit_v18_1.csv should show:
   - raw_predicted_pair_count may still be 76
   - unique_predicted_pair_count may still be 38
   - no_duplicate_pairs_in_predictions should be True for metric predictions
   - duplicate audit-track rows should be explained, not treated as metric self-check failure.

4. summary_audit_v18_1.json should clearly separate:
   - metric deduplication status
   - raw audit duplicate lineage entries

5. C4_context_safety should be either:
   - PARTIALLY_SUPPORTED with qualified Plaid.transaction_id ↔ UPI.txn_id bridge explanation, or
   - SUPPORTED only if a formal, conservative CMNF transaction_identifier policy is implemented and no unsafe cross-context merges remain.
   It should not remain vaguely NOT_SUPPORTED just because the qualified transaction identifier pair appears twice due to duplicate tracks.

6. The patch must not change ground truth, thresholds, schema files, payload files, or expected schema-truth pairs.

OUTPUT FORMAT REQUIREMENT

Return only a downloadable file link for:

  unified_sdnf_experiment_hybrid_v18_1_dbnf_dedup_c4_fix.patch

The file content must be a valid unified git diff patch.

Do not return prose instead of the file.
Do not include markdown explanations unless they are outside the file link and very brief.
Do not generate multiple patch files.